---
phase: 06-context-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/context.ts
  - src/config.ts
autonomous: true
requirements:
  - CTX-01
  - CTX-02
  - CFG-02

must_haves:
  truths:
    - "detectProject returns correct ProjectContext for a Node.js directory with package.json"
    - "detectProject returns null for a directory with no marker files"
    - "detectProject caches results per directory and does not re-scan on second call"
    - "loadProjectConfig reads .claudeshell.json and returns partial config"
    - "loadProjectConfig returns null when file is absent"
    - "loadProjectConfig warns to stderr and returns null for invalid JSON"
    - "mergeConfigs spreads project config over global config"
  artifacts:
    - path: "src/context.ts"
      provides: "Project detection module"
      exports: ["detectProject", "clearContextCache", "ProjectContext"]
    - path: "src/types.ts"
      provides: "ShellState with projectContext and permissionMode fields"
      contains: "projectContext"
    - path: "src/config.ts"
      provides: "Per-project config loading and merge"
      exports: ["loadProjectConfig", "mergeConfigs"]
  key_links:
    - from: "src/context.ts"
      to: "node:fs"
      via: "existsSync + readFileSync for marker detection"
      pattern: "fs\\.existsSync|fs\\.readFileSync"
    - from: "src/config.ts"
      to: ".claudeshell.json"
      via: "loadProjectConfig reads per-project config"
      pattern: "loadProjectConfig"
---

<objective>
Create the foundation modules for Phase 6: project context detection (src/context.ts), per-project config loading (extended src/config.ts), and updated ShellState types.

Purpose: These are the data contracts and pure-logic modules that Plans 02 and 03 consume. No shell wiring yet -- just the building blocks.
Output: Three modified/created files with exported functions ready for integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-context-permissions/06-CONTEXT.md
@.planning/phases/06-context-permissions/06-RESEARCH.md

<interfaces>
<!-- Existing types and contracts the executor needs -->

From src/types.ts:
```typescript
export interface ShellState {
  readonly cdState: CdState
  readonly running: boolean
  readonly lastError: LastError | undefined
  readonly aiStreaming: boolean
  readonly sessionId: string | undefined
  readonly chatMode: boolean
  readonly currentModel: string | undefined
  readonly sessionCost: CostAccumulator
  readonly lastSuggestedFix: string | undefined
}
```

From src/config.ts:
```typescript
export interface ClaudeShellConfig {
  readonly api_key?: string
  readonly model?: string
  readonly history_size?: number
  readonly prompt_template?: string
  readonly prefix?: string
}

export function loadConfig(): ClaudeShellConfig
export function resolveApiKey(config?: ClaudeShellConfig): string | undefined
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types, ProjectContext interface, and context.ts detection module</name>
  <files>src/types.ts, src/context.ts, tests/context.test.ts</files>
  <read_first>src/types.ts, src/config.ts, .planning/phases/06-context-permissions/06-CONTEXT.md</read_first>
  <behavior>
    - Test: detectProject in dir with package.json returns ProjectContext with type "Node.js", name from package.json, markers includes "package.json", summary string
    - Test: detectProject in dir with package.json extracts name, first 10 dependency names, first 10 script names (per D-04)
    - Test: detectProject in dir with Cargo.toml returns type "Rust", no name parsing (per D-05)
    - Test: detectProject in dir with go.mod returns type "Go"
    - Test: detectProject in dir with pyproject.toml returns type "Python"
    - Test: detectProject in dir with requirements.txt returns type "Python"
    - Test: detectProject in dir with no markers returns null
    - Test: detectProject caches result -- second call with same dir does not re-scan (mock fs.existsSync call count)
    - Test: clearContextCache causes next detectProject to re-scan
    - Test: detectProject with malformed package.json still returns type "Node.js" but no name/deps
    - Test: detectProject in dir with multiple markers (package.json + docker-compose.yml) returns primary type "Node.js" with both in markers array
  </behavior>
  <acceptance_criteria>
    - grep -q "export interface ProjectContext" src/context.ts
    - grep -q "export function detectProject" src/context.ts
    - grep -q "export function clearContextCache" src/context.ts
    - grep -q "projectContext" src/types.ts
    - grep -q "permissionMode" src/types.ts
    - npx vitest run tests/context.test.ts passes with 0 failures
  </acceptance_criteria>
  <action>
    1. Update `src/types.ts` (per D-06, D-10, D-18):
       - Add `ClaudeShellPermission` type: `'auto' | 'ask' | 'deny'`
       - Import and re-export `ProjectContext` from context.ts (or define inline)
       - Add to `ShellState`: `readonly projectContext: ProjectContext | null` and `readonly permissionMode: ClaudeShellPermission`

    2. Create `src/context.ts` (per D-01 through D-09):
       - Define `ProjectContext` interface: `{ readonly type: string; readonly name?: string; readonly markers: readonly string[]; readonly summary: string }`
       - Define `MARKERS` array per D-02: package.json->Node.js, Cargo.toml->Rust, go.mod->Go, pyproject.toml->Python, requirements.txt->Python, Gemfile->Ruby, pom.xml->Java, build.gradle->Java, Makefile->C/C++, docker-compose.yml->Docker
       - Implement `detectProject(cwd: string): ProjectContext | null`:
         - Check `contextCache` Map first (per D-09). If key exists (even if null), return cached value.
         - Iterate MARKERS, use `fs.existsSync(path.join(cwd, marker.file))` to find present markers
         - If no markers found, cache null and return null
         - Primary type = first detected marker's type
         - If package.json is among markers, call `readPackageJson(cwd)` to get name, scripts (first 10 keys), deps (first 10 keys) per D-04
         - For all other markers: detect presence only, do NOT parse (per D-05)
         - Build summary string per D-07: "You are in a [type] project called [name]. Key deps: [list]." (omit name/deps parts if unavailable)
         - Cache result and return
       - Implement `readPackageJson(cwd: string): { name?: string; scripts?: string[]; deps?: string[] } | null`:
         - try/catch around readFileSync + JSON.parse
         - Extract name (string), Object.keys of scripts (first 10), Object.keys of dependencies (first 10)
         - Return null on any parse error (per Pitfall 4 in research)
       - Implement `clearContextCache(): void` -- clears the Map
       - Module-level `const contextCache = new Map<string, ProjectContext | null>()`

    3. Write tests in `tests/context.test.ts`:
       - Use `vi.mock('node:fs')` to mock existsSync and readFileSync
       - Call clearContextCache() in beforeEach to reset cache between tests
       - Test all behaviors listed above
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx vitest run tests/context.test.ts</automated>
  </verify>
  <done>ProjectContext type exported, detectProject works with caching, all 11 test cases pass, ShellState has projectContext and permissionMode fields</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-project config loading and merge in config.ts</name>
  <files>src/config.ts, tests/config.test.ts</files>
  <read_first>src/config.ts, .planning/phases/06-context-permissions/06-CONTEXT.md</read_first>
  <behavior>
    - Test: loadProjectConfig returns partial config when valid .claudeshell.json exists with model override
    - Test: loadProjectConfig returns null when .claudeshell.json does not exist (ENOENT)
    - Test: loadProjectConfig warns to stderr and returns null for invalid JSON (per D-24)
    - Test: loadProjectConfig validates fields -- only accepts known fields (model, prefix, permissions, api_key per D-21)
    - Test: loadProjectConfig validates permissions field -- only accepts "auto", "ask", "deny"
    - Test: mergeConfigs returns global when project is null (per D-23)
    - Test: mergeConfigs spreads project over global (per D-20) -- project model overrides global model
    - Test: mergeConfigs preserves global fields not in project config
  </behavior>
  <acceptance_criteria>
    - grep -q "export function loadProjectConfig" src/config.ts
    - grep -q "export function mergeConfigs" src/config.ts
    - grep -q "permissions" src/config.ts
    - npx vitest run tests/config.test.ts passes with 0 failures
  </acceptance_criteria>
  <action>
    1. Extend `ClaudeShellConfig` in `src/config.ts` (per D-12, D-21):
       - Add `readonly permissions?: 'auto' | 'ask' | 'deny'` field
       - Update `loadConfig()` to also validate and include `permissions` from global config file (same pattern as existing fields)

    2. Add `loadProjectConfig(cwd: string): Partial<ClaudeShellConfig> | null` (per D-19, D-24, D-25):
       - Read `path.join(cwd, '.claudeshell.json')` via readFileSync
       - try/catch: if ENOENT return null silently; any other error warn to stderr per D-24 and return null
       - JSON.parse the content; if not an object, return null
       - Validate each field same as loadConfig: api_key (string), model (string), prefix (via validatePrefix), permissions (one of 'auto'|'ask'|'deny')
       - Return only validated fields as partial config
       - Do NOT walk parent directories (per Claude's discretion -- keep simple per D-19)

    3. Add `mergeConfigs(global: ClaudeShellConfig, project: Partial<ClaudeShellConfig> | null): ClaudeShellConfig` (per D-20):
       - If project is null, return global
       - Return `{ ...global, ...project }` -- simple spread per research recommendation

    4. Write/extend tests in `tests/config.test.ts`:
       - Mock fs for loadProjectConfig tests
       - Test all behaviors listed above
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx vitest run tests/config.test.ts</automated>
  </verify>
  <done>loadProjectConfig and mergeConfigs exported, permissions field in ClaudeShellConfig, all 8 test cases pass</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/context.test.ts tests/config.test.ts` -- all tests pass
- `npx tsc --noEmit` -- no type errors
- `grep -q "projectContext" src/types.ts` -- ShellState updated
- `grep -q "permissionMode" src/types.ts` -- permission type in state
</verification>

<success_criteria>
- src/context.ts exists with detectProject, clearContextCache, ProjectContext exports
- src/config.ts has loadProjectConfig, mergeConfigs, permissions field
- src/types.ts ShellState has projectContext and permissionMode
- All tests pass, type check passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-context-permissions/06-01-SUMMARY.md`
</output>
