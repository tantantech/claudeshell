---
phase: 05-pipe-unix-integration
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/config.ts
  - src/classify.ts
  - src/shell.ts
  - src/chat.ts
  - tests/classify.test.ts
  - tests/config.test.ts
autonomous: true
requirements:
  - CFG-01

must_haves:
  truths:
    - "User can set 'prefix' in ~/.claudeshell/config.json and the custom prefix routes to AI"
    - "Default prefix remains 'a' when no config is set"
    - "Chat mode entry works with the custom prefix (bare prefix enters chat)"
    - "Error messages and help text show the configured prefix"
    - "Prefix validation rejects empty strings and strings with spaces"
  artifacts:
    - path: "src/config.ts"
      provides: "ClaudeShellConfig with prefix field and validation"
      contains: "prefix"
    - path: "src/classify.ts"
      provides: "classifyInput accepts prefix parameter"
      contains: "prefix: string"
    - path: "tests/classify.test.ts"
      provides: "Tests for custom prefix classification"
      contains: "custom prefix"
  key_links:
    - from: "src/shell.ts"
      to: "src/classify.ts"
      via: "classifyInput(line, config.prefix ?? 'a')"
      pattern: "classifyInput.*prefix"
    - from: "src/config.ts"
      to: "src/classify.ts"
      via: "config.prefix loaded and passed to classifier"
      pattern: "prefix"
---

<objective>
Make the AI command prefix configurable so users can use `ai`, `claude`, or any custom word instead of `a`.

Purpose: Users who have an existing `a` alias can configure a different prefix without conflict.
Output: Updated config, classifier, shell, and chat modules with prefix threading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pipe-unix-integration/05-CONTEXT.md
@.planning/phases/05-pipe-unix-integration/05-RESEARCH.md

@src/config.ts
@src/classify.ts
@src/shell.ts
@src/chat.ts
@tests/classify.test.ts
@tests/config.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/config.ts:
```typescript
export interface ClaudeShellConfig {
  readonly api_key?: string
  readonly model?: string
  readonly history_size?: number
  readonly prompt_template?: string
}

export function loadConfig(): ClaudeShellConfig
```

From src/classify.ts:
```typescript
export function classifyInput(line: string): InputAction
```

From src/shell.ts (classifier call, line 79):
```typescript
const action = classifyInput(line)
```

From src/shell.ts (error message, line 122):
```typescript
process.stderr.write(`Command failed. Type 'a explain' to ask AI about the error.\n`)
```
Note: Plan 02 replaces this message with auto-fix flow. This plan updates any remaining prefix references.

From src/chat.ts (line 54):
```typescript
process.stderr.write(pc.dim('Chat mode -- type /exit to return to shell, /new for fresh context, /model <name> to switch model\n'))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prefix to config and make classifier accept prefix parameter</name>
  <files>src/config.ts, src/classify.ts, tests/classify.test.ts, tests/config.test.ts</files>
  <read_first>src/config.ts, src/classify.ts, tests/classify.test.ts, tests/config.test.ts</read_first>
  <behavior>
    - ClaudeShellConfig has optional readonly `prefix?: string` field
    - loadConfig() reads `prefix` from config file, validates it is a non-empty string with no whitespace
    - loadConfig() ignores invalid prefix values (returns config without prefix field)
    - classifyInput("ai hello", "ai") returns { type: "ai", prompt: "hello" }
    - classifyInput("ai", "ai") returns { type: "ai", prompt: "" }
    - classifyInput("ai --haiku test", "ai") returns { type: "ai", prompt: "test", model: "claude-haiku-4-5-20251001" }
    - classifyInput("a hello", "a") returns { type: "ai", prompt: "hello" } (default still works)
    - classifyInput("a hello") returns { type: "ai", prompt: "hello" } (no second arg defaults to "a")
    - classifyInput("apt install", "ai") returns { type: "passthrough", command: "apt install" }
    - classifyInput("claude explain this", "claude") returns { type: "ai", prompt: "explain this" }
  </behavior>
  <action>
    1. In `src/config.ts`:
       - Add `readonly prefix?: string` to `ClaudeShellConfig` interface (per D-20)
       - Add a `validatePrefix` helper function: takes `unknown`, returns `string | undefined`. Valid if `typeof value === 'string'`, trimmed is non-empty, and `/\s/.test(trimmed)` is false (per D-23). Return trimmed value or undefined.
       - In `loadConfig()`, add prefix to the config construction: `...(validatePrefix(obj.prefix) !== undefined ? { prefix: validatePrefix(obj.prefix) } : {})`

    2. In `src/classify.ts`:
       - Change signature: `export function classifyInput(line: string, prefix: string = 'a'): InputAction` (per D-21)
       - Replace the hardcoded `'a'` check: change `trimmed === 'a' || trimmed.startsWith('a ')` to `trimmed === prefix || trimmed.startsWith(prefix + ' ')`
       - Update the slice: change `trimmed.slice(2).trim()` to `trimmed.slice(prefix.length).trim()`. Note: when `trimmed === prefix`, slice returns empty string (correct for chat mode entry per D-24). When `trimmed.startsWith(prefix + ' ')`, slice returns everything after the prefix (which starts with a space, then `.trim()` removes it).

    3. Update `tests/classify.test.ts`:
       - Add a new `describe('custom prefix')` block with tests for:
         - `classifyInput("ai hello", "ai")` -> ai with prompt "hello"
         - `classifyInput("ai", "ai")` -> ai with empty prompt (chat mode)
         - `classifyInput("ai --haiku test", "ai")` -> ai with model and prompt
         - `classifyInput("claude explain", "claude")` -> ai with prompt
         - `classifyInput("apt install", "ai")` -> passthrough (not confused by different prefix)
       - Verify existing tests still pass (they use `classifyInput(line)` with default "a")

    4. Update `tests/config.test.ts`:
       - Add test that config with `"prefix": "ai"` loads correctly
       - Add test that config with `"prefix": ""` is ignored (invalid)
       - Add test that config with `"prefix": "my prefix"` is ignored (contains space)
       - Add test that config with `"prefix": 123` is ignored (not a string)
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx vitest run tests/classify.test.ts tests/config.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `ClaudeShellConfig` has `prefix?: string` field
    - `classifyInput` accepts `prefix` parameter, defaults to `'a'`
    - Custom prefix correctly routes to AI, default behavior unchanged
    - Prefix validation rejects empty, whitespace-containing, and non-string values
    - All classifier and config tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Thread prefix through shell.ts and chat.ts</name>
  <files>src/shell.ts, src/chat.ts</files>
  <read_first>src/shell.ts, src/chat.ts, src/classify.ts, src/config.ts</read_first>
  <acceptance_criteria>
    - grep -q "config.prefix" src/shell.ts
    - grep -q "classifyInput(line, prefix)" src/shell.ts || grep -q "classifyInput(line, config.prefix" src/shell.ts
  </acceptance_criteria>
  <action>
    1. In `src/shell.ts`:
       - After `const config = loadConfig()` (line 19), add: `const prefix = config.prefix ?? 'a'`
       - Update the `classifyInput` call (line 79): change `classifyInput(line)` to `classifyInput(line, prefix)`
       - Per D-25: update any error/help messages to use the configured prefix. After Plan 02, the passthrough error block uses auto-fix instead of "Type 'a explain'". If any remaining hardcoded `'a'` references exist in help text, replace with template literal using `prefix`.
       - In the `a fix` detection (added by Plan 02), ensure it uses `prefix` for the help message: e.g., "No fix available. Run a command first.\n" (this message doesn't reference the prefix, so no change needed there).

    2. In `src/chat.ts`:
       - The chat mode help text (line 54) currently doesn't reference the prefix since chat mode is already entered. No change needed here -- chat mode commands are slash-commands (/exit, /new, /model), not prefix-based.
       - However, if `runChatMode` signature or callers need the prefix for any messages, pass it through. Currently not needed since chat mode doesn't use the classifier.

    3. Verify the full flow: `config.prefix ?? 'a'` flows from `loadConfig()` -> `shell.ts` local variable -> `classifyInput(line, prefix)` call.
  </action>
  <verify>
    <automated>cd /Users/tald/Projects/claudeshell && npx tsc --noEmit 2>&1 | tail -10 && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - shell.ts reads `config.prefix` and passes it to `classifyInput`
    - Default prefix `'a'` is used when config has no prefix
    - Help text uses configured prefix (per D-25)
    - TypeScript compiles cleanly
    - All tests pass (no regressions)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run tests/classify.test.ts tests/config.test.ts` -- all prefix tests pass
- `npx vitest run` -- full test suite passes (no regressions)
- `npx tsc --noEmit` -- no type errors
</verification>

<success_criteria>
- Custom prefix configurable via `~/.claudeshell/config.json` `"prefix"` field
- Classifier uses configured prefix instead of hardcoded `"a"`
- Default behavior unchanged (prefix defaults to `"a"`)
- Prefix validated as single non-whitespace word
- Chat mode entry works with custom prefix (bare prefix word)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-pipe-unix-integration/05-03-SUMMARY.md`
</output>
