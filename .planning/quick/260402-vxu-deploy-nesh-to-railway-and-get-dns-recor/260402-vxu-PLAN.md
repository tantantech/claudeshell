# Quick Task 260402-vxu: Deploy nesh to Railway and get DNS records

## Task
Deploy the nesh project to a new Railway project and provide DNS records for nesh.sh domain.

## Plan

### Task 1: Create Railway project and service
- Create new Railway project named "nesh"
- Create service from GitHub repo tantantech/nesh
- Generate Railway domain

### Task 2: Configure custom domain
- Add nesh.sh as custom domain
- Provide CNAME/A records for DNS configuration

## Notes
- This is an infrastructure/ops task, not a code change
- Railway MCP tools used for project/service creation
- Custom domain must be added via Railway dashboard (API limitation)
